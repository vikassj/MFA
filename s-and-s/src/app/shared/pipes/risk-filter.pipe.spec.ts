import { RiskFilterPipe } from './risk-filter.pipe';

describe('RiskFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RiskFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
