import { ObsStatusFilterPipe } from './obs-status-filter.pipe';

describe('ObsStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ObsStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
