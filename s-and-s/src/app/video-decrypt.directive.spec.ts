import { VideoDecryptDirective } from './video-decrypt.directive';

describe('VideoDecryptDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoDecryptDirective();
    expect(directive).toBeTruthy();
  });
});
