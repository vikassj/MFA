import { CameraTypePipe } from './camera-type.pipe';

describe('CameraTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CameraTypePipe();
    expect(pipe).toBeTruthy();
  });
});
